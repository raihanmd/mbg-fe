## 2026-06-11 Decisions

- Footer: convert from styled-components to plain HTML with CSS class names (matching rest of codebase)
- SectionHeading: delete (unused, uses Tailwind classes)
- Skills install buttons: wire to `requestSnap()` for now (same as header buttons)
- Tailwind config: remove tailwind.config.js and postcss.config.js if no longer needed
