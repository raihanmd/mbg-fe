const Positioning = () => (
  <section className="positioning">
    <div className="container positioning-grid">
      <p>No AI private key access</p>
      <p>Bounded delegated permissions</p>
      <p>Optional x402 + Venice AI reasoning</p>
    </div>
  </section>
);

export default Positioning;
